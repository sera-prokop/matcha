mixin menu-btn(data)
    .menu-btn
        a(href="#").menu-btn__button Каталог товаров
        +burger('Показать меню', 'menu-catalog', 'menu-catalog--open')(class='')
        +menu-catalog(menu_catalog)

extends _template.pug

include ../components/head/head

include ../components/main-descr/main-descr
include ../components/main-catalog/main-catalog
include ../components/all-actions/all-actions



block head
    +head(head.actions)

block content
    main
        +breadcrumbs()
            +breadcrumbs-item('Главная', '/')
            +breadcrumbs-item('Акции')
        +cart-day(cart_day)
        +all-actions(all_actions)
        +all-popular(popular.popular)
        +set-banner(set_banner)
        +text-block()
        +instagram(instagram)




